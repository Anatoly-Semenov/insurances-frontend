export namespace UiCheckbox {
	export type Type = "switch" | "checkbox"

	export enum TypeEnum {
		SWITCH = "switch",
		CHECKBOX = "checkbox"
	}

	export type Size = "small" | "default"

	export enum SizeEnum {
		SMALL = "small",
		DEFAULT = "default"
	}
}
