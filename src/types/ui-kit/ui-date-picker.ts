export namespace UiDatePicker {
	export type Size = "large" | "middle" | "small"

	export enum SizeEnum {
		LARGE = "large",
		MIDDLE = "middle",
		SMALL = "small"
	}

	export type Picker = "date" | "week" | "month" | "quarter" | "year"

	export enum PickerEnum {
		DATE = "date",
		WEEK = "week",
		MONTH = "month",
		QUARTER = "quarter",
		YEAR = "year"
	}
	export type Mode = "time" | "date" | "month" | "year" | "decade"

	export enum ModeEnum {
		TIME = "time",
		DATE = "date",
		MONTH = "month",
		YEAR = "year",
		DECADE = "decade"
	}
}
